Adopted the agentics template in this repo so any agent working here picks up the same conventions already in use across the other Overture repos, and seeded the `.dev/` working documents.

- `AGENTS.md`: canonical agent-neutral conventions, project-specific only: repo context, the two content systems, constraints (public repo, pinned Node 16 / Gatsby 4 toolchain, no test suite, never deploy, links centralized in `constants/`), and a repository orientation. Universal conventions, the developer role, the softeng layer, and the Overture layer are dispatched to the agentics template rather than copied in. Tagged `agentics-template-version: 0.12.0 | synced: 398df9c`
- `CLAUDE.md`: stub pointing at `AGENTS.md`
- `.dev/roadmap.md`: current state read off git history, plus one open question: `/documentation` here and `docs.overture.bio` both serve documentation for the same components, with nothing recording which is canonical
- `.dev/tech-debt.md`: seeded with the documentation-section findings turned up while writing the orientation (stale search wiring, wrong paths in `README-documentation.md`, Ego still presented as current)
- No `DEVELOPMENT.md` and no `.claude/settings.json`: `README.md` and `README-documentation.md` already cover setup, and the credential-blocklist hook is already in place globally, so both would be second copies

---

Audited the site for cleanup and upgrade candidates. Everything below was verified against the tree, the installed packages, or the live site rather than inferred; the two toolchain failures are the ones that matter, since the rest is either blocked behind the upgrade or cheap once tooling runs again.

- `.dev/roadmap.md`: three-phase toolchain upgrade (unblock install and lint, then Gatsby 5 and React 18/19, then everything those unlock), plus a second open question on whether the Gatsby migration is worth doing at all if this site is eventually folded into the Docusaurus stack
- `.dev/tech-debt.md`: 27 entries, ordered by severity. Toolchain: `npm install` aborts with `EBADENGINE` on Node 20 because `.npmrc` sets `engine-strict` against a Node 16 `engines` range, and `npm run lint` cannot start because `.eslintrc.js` references three ESLint packages that were never installed while the installed `standard` configs go unused, which is why the dead code below accumulated unnoticed. Security: 125 advisories, 8 critical, mostly transitive through Gatsby 4, of which `sharp`, `lodash`, and deleting the unused `gatsby-source-git` are fixable without the major upgrade. Analytics: Matomo's `dev` flag is inverted, so local development traffic reaches production statistics with DNT ignored. Dead weight: six unused dependencies, one undeclared (`dotenv`), six dead components plus two they orphan, a published Netlify CMS config left over from the starter template, and three inert settings in `meta/config.js`. Site correctness: `siteUrl` names the apex host that 301s to `www`, there is no `robots.txt` and the sitemap sits at the plugin's default path, 15 `<img>` tags lack `alt` including the shared `Icon`, four maestro nav URLs render a doubled slash, six docs pages are unreachable from any nav, and `markdown/` carries 60MB of uncompressed screenshots
- Not verified, called out rather than assumed: whether Matomo's cookie configuration matches what the privacy page implies, and whether the Gatsby 4 build runs on a supported Node at all, which is why phase 1 of the roadmap ends at a verified pairing rather than a version bump

---

Recorded the decision that `docs.overture.bio` is the only Overture documentation surface, and planned the retirement of the `/documentation` section here. Scoping it showed the marketing side has already migrated, so the work is smaller than the page count suggests: the getting-started hub already links out to the docs site, and every constant in `constants/docs.js` plus the four `*_GUIDE` constants in `constants/pages.js` have no consumers left. The navbar megamenu's ten hardcoded paths are the only live entry point.

- `.dev/roadmap.md`: the open question became a decision, and phase 2 is now the retirement: redirect and repoint the megamenu first, then delete the pipeline, with an inbound-link check against Matomo before deleting. Redirect targets go to section front doors rather than per-page equivalents, because `docs.overture.bio` still serves the old `/docs/core-software/` and `/guides/` IA live while the journey routes (`/deploy`, `/use`, `/develop`) 404, so a deep-link map would break when that ships. Ego and the DMS bundle have no successor pages, so they point at the authorization guide and the Quickstart tool respectively. Retiring the section before the framework upgrade shrinks that upgrade: the MDX and YAML plumbing is where Gatsby 5 would most likely break, and 59 Markdown files depend on the shortcode layer
- `.dev/tech-debt.md`: pruned seven entries that the decision resolves outright (stale search wiring, `README-documentation.md` paths, doubled-slash maestro nav URLs, six pages missing from nav, mixed `.yml`/`.yaml` manifests, 60MB of docs screenshots, the 2020 rehydration workaround), narrowed the Ego and external-links entries to what survives on the marketing side, and added one: the same `keywords` meta string is duplicated across seven pages and still advertises the retired DMS
- Deferred to the owner rather than assumed: what the getting-started page becomes once it has no on-site documentation to hub for, logged as an open question

---

Reviewed `overture-stack/docs#48` (the docs v2 journey restructure) and realigned this repo's phase 2 to it, since the first redirect table targeted the IA that PR replaces.

- `.dev/roadmap.md`: redirect targets moved to the new journey routes (`/develop/<Component>/overview`, `/deploy/deployment`, `/deploy/prelude`, `/use/cli-submissions`, `/use/cli-downloads`, `/deploy/deployment/keycloak`), each confirmed present in a local production build of that branch. Phase 2 is now explicitly blocked on that PR merging: the targets do not exist on the live site yet, and the alternative of pointing at today's paths would chain a 301 from here into a meta-refresh page there, since the old paths survive only as `plugin-client-redirects` output
- Review findings for that PR are drafted, not posted. The substantive one is that 54 of the 106 URLs currently in the live sitemap resolve to nothing in the new build, mostly the component `usage/` pages and the `docs/documentation-standards/*` subtree: `createRedirects` can only emit a redirect for a path that still exists, so pages renamed or retired upstream lose their `/docs/core-software/...` entry point, and `onBrokenLinks: throw` cannot catch it because these are external URLs, not in-build links
- Independently verified rather than taken from the PR checklist: the production build is clean (exit 0, 382 pages, zero warnings, `onBrokenLinks: throw`), and all eight submodule pins point at commits reachable only from each submodule's own `overtureDocsUpdate` branch, not from any default branch, though Arranger and Stage have since merged upstream so those two pins can be re-pointed now

---

Carried out phase 1: the repo installs, lints, builds, and runs a dev server on Node 22, which it could not do on any supported runtime before. The verified pairing is Node 22.14.0 with npm 10.9.2 against Gatsby 4.25, established by running each path rather than reasoning about version ranges.

- `package.json`, `.nvmrc`: `engines` moved to Node `>=22.0.0` and npm `>=10.0.0`, with `.nvmrc` pinning 22. Node 20 was rejected as the target because it left LTS maintenance in April 2026. `.npmrc`'s `engine-strict=true` stays and now does something useful: an install on Node 16 fails immediately, naming both the required and actual versions
- `eslint.config.mjs` replaces `.eslintrc.js`: ESLint 9 flat config with `eslint-plugin-react`, `eslint-plugin-react-hooks`, and `eslint-config-prettier`; nine stale lint packages including `babel-eslint` removed, and the `lint` script drops the `--ext` and `--ignore-path` flags ESLint 9 rejects. ESLint 10 was the first attempt and cannot be used yet: no released `eslint-plugin-react` accepts it, its peer range stopping at `^9.7`. `react/prop-types` is off, since it produced 279 of the first run's 306 errors and enabling it in earnest means annotating every component
- `gatsby-node.js`: strips Gatsby's own ESLint plugin from the develop bundle. Gatsby 4 injects it and reads only eslintrc-style config, so once `.eslintrc.js` was gone it fell back to its built-in rules and failed the dev bundle outright. Removing it leaves one lint authority instead of two that can disagree
- `src/components/LinkHelper/index.js`: external links were opening with `target="_blank"` and no `rel`, exposing every outbound link on the site to reverse tabnabbing; now `rel="noopener noreferrer"`, confirmed present in built HTML
- `src/hooks/useActiveId.js`: hooks ran after an early return, so hook order depended on whether the table of contents had entries; the guard moved inside the effect, which preserves the behaviour of observing nothing when the list is empty
- `src/components/Button/index.js`, `src/components/Layout/index.js`: removed duplicate `default` keys in two lookup objects (both duplicates held the same value, so no behaviour change) and a no-op `this.popOverRef;` statement whose ref is actually assigned in `render`
- Verified: `npm ci` from a deleted `node_modules` (exit 0), `gatsby build` (exit 0, 131 pages, 784 images, sitemap present), `gatsby develop` (serves, zero errors in its log), and lint running to a real baseline of 22 errors and 89 warnings, logged in `tech-debt.md`. `sharp` and `lmdb` needed no rebuild for the new Node major, since both are N-API addons
- Not verified: which Node version Netlify uses for the production build. `.nvmrc` should be picked up, but with `engine-strict=true` an older Netlify default would now fail the install instead of building on the wrong runtime, so the first deploy preview needs watching; the fallback is `NODE_VERSION` in `netlify.toml`

---

Carried out phase 2: the `/documentation` section and the pipeline behind it are gone, and every documentation link on the site now points into the docs v2 journeys. The site is a marketing site with no content pipeline: 14 built pages instead of 131, and a 12 second production build instead of 62.

- `netlify.toml`: twelve 301 redirects from `/documentation/*` to their nearest docs.overture.bio equivalent, `force = true` so no stale published file can shadow them, catch-all last because Netlify matches in file order. Also set `NODE_VERSION = "22"`, which closes the phase 1 gap about Netlify's implicit Node, and removed the inert Yarn variables
- Deleted `markdown/documentation/` (118 pages, 60MB), `src/templates/` (the documentation template and a dead Gatsby v1 era wrapper nothing imported), `DocsWrapper`, eight components, three hooks, `meta/algolia-queries.js`, `constants/docs.js`, `README-documentation.md`, and 27 dependencies. `gatsby-plugin-sharp` and `gatsby-transformer-sharp` went too, which the plan had not anticipated: nothing queries `childImageSharp`, so image processing existed only for Markdown
- Links repointed to v2 routes through a single `DOCS` base in `constants/external-links.js`, consumed by the megamenu, the products page section, the homepage, and the getting-started page. Auditing the built HTML rather than the source caught two link sites the plan had missed: `ProductsPageSection` built its own `docs/core-software/${title.toLowerCase()}/overview/` URL, which was doubly wrong since those routes are capitalized, and the homepage hardcoded `documentation/stage`
- `useSSRWorkaround` was kept: the plan had it as documentation-only, but `MegaMenuLink` uses it
- Deleting `sharp`, `lodash`, and `gatsby-source-git` outright resolved the standalone security entry that proposed bumping them. Advisories fell from 118 to 87, criticals from 7 to 2, direct-dependency advisories from 21 to 8
- Two logged items were fixed in passing, since they lived in files being rewritten anyway: Matomo's `dev` flag no longer sends local development hits to production statistics, and the sitemap now builds at `/sitemap-index.xml` instead of a path nothing links to
- Removed one content bullet from the getting-started page: its "Under Development" link named a docs category that v2 does not have, so it would have 404'd either way. Flagged for whoever owns the page copy
- Verified: lint (15 errors, 68 warnings, down from 22 and 89), `gatsby build` (exit 0, 14 pages, no `/documentation` link left in the output), `gatsby develop` (serves, zero errors), and all 25 docs URLs now referenced by the site or the redirects present in a local production build of the docs v2 branch
- Deployment constraint recorded in `roadmap.md`: this must not reach production before the docs v2 PR is live, since every one of those targets 404s on docs.overture.bio today

---

Recorded two decisions and replaced the framework-upgrade plan with a consolidation plan, after measuring what Gatsby still provides here.

- `.dev/roadmap.md`: the getting-started page stays as a curated entry page, which makes rewriting its pre-v2 "Core Software" framing into the four docs journeys a content task rather than an open question. The larger decision is that this repo consolidates into `overture-stack/docs` and the Gatsby 5 and React 18 upgrade is cancelled, along with the ESLint, Prettier, and Bulma majors that depended on it
- The measurement behind it: zero GraphQL queries remain after phase 2 (`useStaticQuery`, `StaticQuery`, and `graphql` tags all count zero) and case-study data is a plain import, so Gatsby's data layer, image pipeline, and MDX are all unused, while the build still ships 1.3MB of JavaScript across 28 files for 13 static pages
- Phase 3 is now four stages: port the pages into the docs repo with no DNS change, review on a branch deploy, cut the domain over to `overture.bio` with path-for-path redirects from `docs.overture.bio`, then archive this repo. The chrome (`Layout`, `NavBar`, `MegaMenu`, `Footer`, `useSSRWorkaround`) is not ported at all, since Docusaurus supplies it, and the dead components are deleted by not porting them
- Facts that shaped the plan, checked in the docs repo rather than assumed: it runs Docusaurus 3.10 on React 18 with `onBrokenLinks: "throw"`, already serves a custom `src/pages/index.tsx`, has no Sass support so the 39 SCSS files need `docusaurus-plugin-sass` added, and already tracks Matomo siteId 76, the same site this repo reports to, so analytics needs nothing. Both sites are on Netlify, making the cutover DNS and redirects rather than a hosting migration
- The main risk is named as CSS, not JavaScript: Bulma's global resets will fight Infima, so marketing styles get scoped under a wrapper class rather than rewritten onto Infima variables in the same change
- `.dev/tech-debt.md`: entries now carry a pointer to check the consolidation plan first, since the move resolves nine of them outright and four follow the content to its new home

---

Split the remaining work so the aligned Gatsby site ships before any porting begins, and the port starts from production rather than a branch.

- `.dev/roadmap.md`: a new phase 3 verifies and ships the docs-aligned Gatsby site, and the consolidation became phase 4, on its own branch, starting only once phase 3 is deployed. Porting from what production actually serves keeps the two copies from diverging and makes the port a translation rather than a translation plus a merge
- Phase 3 names something the earlier work did not cover: alignment has two halves, and only the mechanical half is done. Links resolve, but the megamenu still offers "Platform Guides" and "Product Documentation" while the docs site is now Develop, Deploy, Use, and Community, so a visitor meets one vocabulary here and another on arrival. Reworking the megamenu and curating the getting-started page are the second half
- Also recorded: the link verification done on 2026-08-06 is provisional, because five component PRs still have to merge and their submodule pins be re-pointed, and re-pointing a pin can change component page slugs. The check to keep is mechanical (extract every docs URL from the built site and `netlify.toml`, require a 200 from each) and gets run three times: against the docs branch build, against live docs after merge, and against this site's deploy preview
- Rollback recorded for the case where `docs#48` is reverted after this ships: repoint the redirects at the docs site root rather than reverting the deploy

---

Trimmed the roadmap to forward-looking work only, so it reads as a plan rather than a record: the two shipped phases and the pre-work plan they were written against came out, leaving the current state, the decisions that still govern what happens next, the two pieces of remaining work, and one open question. From 175 lines to 89.

- `.dev/roadmap.md`: shipped work now lives only in this log and git history, per `session-discipline.md` on collapsing completed work. Phase numbering went with it, since the numbers had already been reused once and would have read ambiguously against older entries; the two remaining pieces are named by what they do. The getting-started resolution moved from Open questions into Decisions, where a settled call belongs
- `AGENTS.md`, `.dev/tech-debt.md`: three cross-references pointed at sections that no longer exist. They now name the Decisions section and the consolidation section instead. The `AGENTS.md` toolchain rule also said the Gatsby 5 upgrade was pending work; it now states plainly that the upgrade is cancelled and that work here is maintenance on a stack with a known end date

---

Finished the alignment work by fixing what a readiness check turned up, and found that one part of it was aimed at code nobody can reach.

- `src/components/ProductsPageSection/index.js`, `styles.scss`: product section ids were derived from a viewport measurement, so the built page carried `id="false"` eight times and none of `id="song"`, `id="score"`, `id="maestro"`, `id="arranger"`. Every homepage product tile linking to `/products/#song` and friends scrolled nowhere. Each section now carries its id unconditionally, with `scroll-margin-top` keeping the heading clear of the fixed navbar, which is what the viewport branching was approximating. Verified in the built HTML: four real ids, zero `id="false"`
- `src/pages/index.js`, `constants/pages.js`, `constants/products.js`: removed the homepage Ego tile, which advertised a discontinued product through the `/products/#ego` anchor the products page never had, and the matching anchor entry. `constants/products.js` turned out to have no importers left at all once the documentation pipeline went, so the whole file is deleted, which resolves the retired-products entry as well. The remaining Ego mentions are historical (case-study descriptions, the acknowledgements page) and stay
- `src/pages/getting-started/index.js`: the resources section now presents Deploy, Use, Develop, and Community as the four ways into the documentation, each with a line on what it is for, replacing "Platform Guides" and a single "Core Software" link. This is the page the Decisions section named as the site's curated handoff, so it is the one that mattered
- `src/components/NavBar/MegaMenu/index.js`, `constants/external-links.js`: megamenu sections reworked to Develop, Deploy, and Use, with Lectern, Lyric, Prelude, and the API reference added. **This is cosmetic:** `NavBar` renders plain `NavLink`s and never calls `toggleMegaMenu`, so the megamenu only ever returns its empty animation div, and "Explore our documentation" appears zero times in the built homepage. `MegaMenuLink`, which would open it, is imported by nothing. Logged in `tech-debt.md` along with the `Layout` state and the `useSSRWorkaround` hook that exist only to serve it, and it corrects an earlier note claiming that hook had to stay
- Verified after the changes: lint unchanged at 15 errors and 68 warnings, `gatsby build` exit 0, and all 20 distinct docs URLs the site now references present in a production build of the docs v2 branch, up from 17 because the megamenu added Lectern, Lyric, Prelude, and the API reference
